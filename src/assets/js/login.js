const login = async (email,senha)=>{
   //faz a requisição para a rota de login
   //que fica em http://localhost/app-catalogo-livros-tsi/api/usuarios/login

   //retorna a resposta da requisição
}

export default login;