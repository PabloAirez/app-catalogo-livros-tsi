const cadastrar = async (nome,email,senha)=>{
    //faz a requisição para a rota de cadastro
    //que fica em http://localhost/app-catalogo-livros-tsi/api/usuarios

    //retorna os dados do usuário cadastrado ou a resposta de erro
}

export default cadastrar