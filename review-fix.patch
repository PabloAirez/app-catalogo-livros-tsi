--- a/src/pages/dashboard.jsx
+++ b/src/pages/dashboard.jsx
@@ -438,8 +438,13 @@ const Dashboard = () => {
         // Busca a avaliação existente para saber se é PUT ou POST
         const existingReview = await fetchBookReview(livroId);

-        // Não precisa esperar o saveReview bloquear o fluxo principal
-        saveReview(rating, review, livroId, existingReview.id);
+        // AGORA ESPERA A SAVE REVIEW COMPLETAR
+        const result = await saveReview(rating, review, livroId, existingReview.id);
+        
+        // Se salvou com sucesso, usa os dados retornados (garantia de sincronia)
+        if (result) {
+            savedReviewData = { nota: result.nota, comentario: result.comentario };
+        }
     }

     const normalizedBook = {
@@ -453,8 +458,8 @@ const Dashboard = () => {
           : '',
         publisher: livroSalvo.editora || '',
         status: livroSalvo.status || 'none',
-        // Usa os valores do form para garantir atualização imediata na lista
-        rating: rating || 0, 
-        review: review || '', 
+        // Usa os valores confirmados da API ou do form
+        rating: savedReviewData.nota || 0, 
+        review: savedReviewData.comentario || '', 
         url_capa: livroSalvo.url_capa || '',
         descricao: livroSalvo.descricao || '',
@@ -433,6 +433,7 @@ const Dashboard = () => {
     const livroId = livroSalvo.id || formData.id;
     const rating = formData.rating;
     const review = formData.review;
+    let savedReviewData = { nota: rating, comentario: review };
     
     if (rating > 0 || review) {
